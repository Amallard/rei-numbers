(function (angular) {
    'use strict';

    var appFooter = {
        bindings: {
        },
        templateUrl: 'app/app-footer.html'
    };

    angular
        .module('app')
        .component('appFooter', appFooter);

}(window.angular));
